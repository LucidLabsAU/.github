# .github

Org-wide community health files, workflow templates, and Copilot defaults for the LucidLabsAU organisation.

## Reusable workflows

- [`macos-app-release.yml`](.github/workflows/macos-app-release.yml) — build, sign, notarise and package SwiftPM macOS apps (zip + `/Applications` pkg). Runbook: [docs/releasing-macos-apps.md](docs/releasing-macos-apps.md).
